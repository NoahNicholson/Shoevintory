@model Shoevintory.Models.CollectionDetailsViewModel

@{
    ViewData["Title"] = "Details";
}

<h1>Details</h1>

<div>
    <h4>CollectionDetailsViewModel</h4>
    <hr />
    <dl class="row">
        <dt class = "col-sm-2">
            @Html.DisplayNameFor(model => model.CollectionId)
        </dt>
        <dd class = "col-sm-10">
            @Html.DisplayFor(model => model.CollectionId)
        </dd>
        <dt class = "col-sm-2">
            @Html.DisplayNameFor(model => model.CollectionName)
        </dt>
        <dd class = "col-sm-10">
            @Html.DisplayFor(model => model.CollectionName)
        </dd>
        @foreach (var s in Model.Shoes)
        {
            <p>@s.Name</p>
        }
        <a asp-controller="ShoeCollection" asp-action="Create" asp-route-id="@Model.CollectionId">Add Shoe</a>
    </dl>
</div>
<div>
    @Html.ActionLink("Edit", "Edit", new { /* id = Model.PrimaryKey */ }) |
    <a asp-action="Index">Back to List</a>
</div>
